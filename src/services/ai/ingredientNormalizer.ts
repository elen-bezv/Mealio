/**
 * Central AI / Ingredient engine: canonical names (dictionary + fuzzy).
 * Re-exports from lib/ingredient-normalize.
 */

export {
  normalizeIngredientName,
  getCanonicalKey,
  INGREDIENT_DICTIONARY,
} from "@/lib/ingredient-normalize";
export { normalizeIngredientNameAI } from "@/lib/ingredient-normalize";
