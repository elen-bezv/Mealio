/**
 * Central AI: Recipe Parser Pro — URL, Instagram, TikTok, image, PDF, text → StructuredRecipe.
 * Re-exports from services/recipe-parser-pro.
 */

export {
  parseFromText,
  parseFromUrl,
  parseFromImage,
  parseFromPdf,
  findDuplicateTitle,
} from "@/services/recipe-parser-pro";
